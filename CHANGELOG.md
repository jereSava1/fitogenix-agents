# CHANGELOG — Fitogenix

> **Único escritor: el Orquestador.** Historia del SSOT: qué cambió, cuándo y contra qué se
> verificó. Salió de `CONTEXT.md §9` el 2026-09-03, por la regla propia del proyecto —
> *estado vigente en `CONTEXT.md`, historia afuera*. Eran 18 KB (el 27 % del SSOT) que solo
> 2 de 10 agentes citaban, y que ningún agente de disciplina necesita nunca.
>
> Una decisión de arquitectura además se registra como ADR en `BITACORA_DECISIONES.md`.

> **Único escritor: el Orquestador.** Un cambio de este documento se registra acá con
> fecha, qué sección cambió y contra qué se verificó. Una decisión de arquitectura además
> se registra como ADR en `BITACORA_DECISIONES.md`; acá va la línea de que se registró.

| Fecha | Cambio | Verificado contra |
|---|---|---|
| 2026-08-28 | Creación. §1–§8 armados desde `AUDITORIA_SETUP_AGENTICO.md`, `DICCIONARIO_DOMINIO.md` (ya corregido) y `tareas/FTG-001`. Números de §6 tomados de FTG-001 sin recalcular | `~/fitogenix-server` `main` `a0428bd` · `~/fitogenix-native` `main` |
| 2026-08-28 | Contradicciones nuevas encontradas en la verificación de esta sesión: **C-07** (cascada retirada, docs desactualizados), **C-08** (composición del score describe v2), **C-09** (rol de NOVA), **C-10** (cola de curaduría descartada), **C-11** (puntaje sin cobertura). Todas en §8 | `productLookupService.ts` · `scoring/constants.ts` · `scripts/audit-scores.ts` |
| 2026-08-28 | **Convención de marcas ampliada a cuatro:** se suma 🟡 (decidido, no implementado) a ✅/⚠️/🔴. ⚠️ = no verificado; 🟡 = verificado como ausente y ya decidido | — |
| 2026-08-28 | **Tres huecos del SSOT cerrados** (autorizados por Jere): §1.6 estado real de pantallas y features · §5.7 selección de modelo de IA · §5.8 stack del cliente. **Ninguna sección existente se renumeró** — los punteros `§X` de los 8 agentes siguen válidos | `fitogenix-native` `b7715b8` · `claudeService.ts` |
| 2026-08-28 | **C-09 mapeado, no cerrado:** NOVA se ingiere, se persiste, lo usa `audit-scores.ts` como señal de calidad y se le nombra al usuario, pero el motor no lo lee. B-4 se desdobla en B-4 (C-08) y B-4b (C-09) | `steps.ts`/`pipeline.ts` · `audit-scores.ts` (chequeos `nova_group === 4` / `=== 1`) · `HelpScreen.tsx` |
| 2026-08-28 | **C-07 cerrado.** Corregido en `00-orquestador.md` (reescrito), `02-agente-frontend.md` (reescrito), `03`, `06` y el `README.md` de `fitogenix-server` — que documentaba la cascada como *la* arquitectura y decía "Unit tests (119)" cuando hay 27 archivos y ~345 casos | grep en los 9 documentos + `productLookupService.test.ts` |
| 2026-08-28 | **C-14 nuevo (B-13):** el copy de `HelpScreen.tsx` describe el motor v2 y la cascada retirada. Primer caso de deriva que llegó al usuario final. **C-15 nuevo (B-14):** la poda de dependencias de la Fase 2 ya se hizo sin registrarse | `HelpScreen.tsx` · `fitogenix-native/package.json` |
| 2026-08-31 | **Convención de citas al código:** archivo + símbolo o cita textual, nunca número de línea. Se convirtieron los 10 punteros con línea del set vivo. Encontrado al aplicarla: `ENGINE_VERSION` se citaba como `ftgEngine.ts:24` y vive en `scoring/constants.ts:24` — archivo equivocado, número correcto, error invisible durante tres días. Los rangos de línea quedan permitidos solo en reportes fechados | `HelpScreen.tsx` · `ProfileScreen.tsx` · `ingredientData.ts` · `audit-scores.ts` · `scoring/constants.ts` |
| 2026-08-31 | **Decisión 1 — tier inicial gratuito.** §4.3 reescrita entera: de 🟡 *lookup con cuota* a ✅ *tier inicial gratuito*. El encuadre pasa de **deuda a diseño del MVP** — el endpoint público ya no tiene fecha de vencimiento. §4.1 y §4.2 alineadas (§4.2 marcada explícitamente como no-MVP), §8 B-1 cerrado, los 7 ítems del gap eliminados. **Revierte la decisión del 28/8**, cuya entrada sale de este changelog: la evolución vive en `BITACORA_DECISIONES.md` | ✅ `src/routes/products/lookup.ts` · `src/routes/users/history.ts` |
| 2026-08-31 | **Decisión 2 — NOVA se sostiene.** §2.4 reescrita: las tres formas en que NOVA participa, y ninguna es el puntaje. §8 B-4b cerrado como *sostenido*. Se registra por primera vez el uso de `audit-scores.ts` como señal de calidad — la razón operativa más fuerte para conservar el campo, que ningún documento tenía. **La limpieza de código queda descartada.** B-13 se desbloquea (era 🔴 bloqueado por B-4b, ahora 🟡) | ✅ `steps.ts`/`pipeline.ts`/`rubric/` · `audit-scores.ts` · `scoring/types.ts` |
| 2026-08-31 | **Decisión 3 — pantalla de producto fuera de catálogo.** Nuevo §8 B-16, documentado en §1.6. Hallazgo al verificar: `scan_failed`, que `02-agente-frontend.md` daba por atado a esa distinción, **no existe en el código** | ✅ `client.ts` · `lookup.ts` · grep en `fitogenix-native/src/` |
| 2026-08-31 | **Corrección de un error de esta misma sesión, y anterior a ella.** Se había escrito que el 404 del lookup se tipifica como `ProductNotInCatalogError` y que nadie lo consume. **Es falso:** `lookupProduct()` devuelve `null`, y ese error lo lanza solo `saveProductRemote()`. El caso fuera-de-catálogo **sí se detecta** en los dos hooks; lo que fallaba era la presentación y el copy. El error venía de `REALINEACION_REPORTE.md` (28/8), que ató `scan_failed` a `ProductNotInCatalogError`, y se propagó sin verificarse contra el call site | ✅ `client.ts` → `lookupProduct` / `saveProductRemote` · `useScanFlow.ts` · `useProductSearch.ts` |
| 2026-08-31 | **Nuevo §8 B-15 — el anónimo persiste y no debería.** `scanResultStore.tsx` hidrata desde AsyncStorage sin mirar sesión. Se cierra además la sub-decisión que el 28/8 quedó viva: los escaneos de la sesión **se migran** al historial si el anónimo se registra en esa sesión. Verificado que el backend no acepta escrituras de historial sin usuario | ✅ `scanResultStore.tsx` · `users/history.ts` (requireAuth, solo GET) |
| 2026-08-31 | **B-15 y B-16 cerrados: el código, no solo la documentación.** El anónimo ya no persiste, el deslogueo borra el disco con `multiRemove`, y la migración anónimo→logueado re-emite los lookups con token. Fuera de catálogo tiene cartel propio, separado del error de red. **Queda 🟡 solo el copy de las dos pantallas**, a cargo de UX | ✅ `scanResultStore.tsx` · `anonScanMigration.ts` · `ProductNotInCatalogCard.tsx` · `useScanFlow.ts` |
| 2026-08-31 | **Primer módulo de analítica del cliente (`src/analytics/`) y `scan_failed` implementado.** Una sola función tipada, `snake_case`, cero PII, no-op real sin consentimiento. El evento registra el barcode o nombre del producto, su tipo, el motivo, el origen y la fecha del escaneo. **Nuevo B-17:** el sink no está conectado a ningún SDK, así que la métrica de cobertura de catálogo todavía no se mide | ✅ `src/analytics/index.ts` · `analytics-events.ts` |
| 2026-08-31 | **El cliente dejó de no tener tests: 49 en verde.** `@testing-library/react` + `jsdom`, con `react-native` aliasado a `react-native-web` (que ya era dependencia). No se usó `@testing-library/react-native`: necesita `react-test-renderer`, deprecado en React 19, y que el runner transforme el Flow sin transpilar de `react-native`. **Testea render web, no plataforma nativa** — no reemplaza device ni Detox | ✅ `vitest.config.ts` · 5 archivos de test |
| 2026-08-31 | **CI de `fitogenix-native` roto por el Node del runner, y arreglado** (§8 B-18). El workflow corría Node 20 y `jsdom@30` pide `^22.22.2` — los tres tests de UI no arrancaban el worker. Es deuda preexistente que se hizo visible ahora: `react-native@0.85` ya pedía Node 20.19.4+ y el repo no tenía `engines` para decirlo. La versión pasa a vivir en `.nvmrc`, no en un literal del YAML que se desincroniza en silencio | ✅ `npm ci` limpio sobre el mismo `package-lock`: 49 en verde, `tsc` limpio |
| 2026-08-31 | **Nace el Agente de Nutrición** (`09-agente-nutricion.md`) con su SSOT propio `nutricion/NUTRICION.md` (§N1–§N7). Cierra el rol que §7 marcaba como faltante y que B-12 señala como causa raíz de otros cuatro bloqueantes. Nace **vacío y con contrato duro**: cita fuente primaria o devuelve 🔴, y distingue ✅ (primaria) de 📄 (prensa) — un 📄 nunca alcanza para cambiar código | `Ley 27.642` art. 7 · perfil de nutrientes de OPS · `scoring/seals.ts` |
| 2026-08-31 | **B-11 avanza: 4 de 5 umbrales de octógonos verificados** contra el perfil de OPS. Los cuatro coinciden exactamente; el de calorías queda 🔴 porque el Anexo II del Decreto 151/2022 no está accesible. Verificado además que la ausencia de octógono de grasas trans **es correcta** (la ley no lo incluye; OPS sí). Registrado en el encabezado de `seals.ts`, donde se usa | `fitogenix-server` `0712f2d` |
| 2026-08-31 | **Corrección de un error de análisis, registrada porque el método importa.** Se reportó que `seals.ts` no implementaba la excepción del art. 7. **Es falso:** está en `steps.ts` → `applyNutrition`, un nivel más arriba. El error salió de concluir sobre un archivo aislado sin verificar el call site — el mismo método que produjo C-07 y C-14. Es el caso que justifica el contrato del agente nuevo, y está citado en su prompt | ✅ `scoring/steps.ts` |
| 2026-08-31 | **B-11 avanza mucho más: aparece la Tabla 1 del Decreto 151/2022** con los valores de las dos etapas (`NUTRICION.md §N3`). Los umbrales viven en el **art. 6**, no en el Anexo II. Dos discrepancias con el código, ninguna aplicada todavía por falta de fuente del BORA (`§N6` N-5 y N-6). Corregido además que **la Disp. 11378/2024 es de publicidad, no de criterio** — la que aprueba el Manual de Aplicación es la 11362/2024, y **no cambia los valores de corte** | InfoLEG `362577` y `407673` · BORA `318799` · Anexo I (editorial jurídica) |
| 2026-08-31 | **B-11 cerrado, y con dos correcciones al motor.** La calculadora oficial de ANMAT confirmó la Tabla 1: al sodio le faltaba la condición `≥300 mg/100 g` y el corte de calorías de bebidas era 70 en vez de 25. **Los dos sub-marcaban** — se mostraban menos octógonos de los que el envase lleva, que es el peor error posible para el único dato que el usuario puede verificar (§2.5). `ENGINE_VERSION` → `ftg-rubric-v2.2` | ✅ `fitogenix-server` `seals.ts` · 415 tests |
| 2026-08-31 | **B-11 cerrado de verdad, con el Manual de Aplicación oficial** (60 págs., `IF-2024-135393117`). Corrige el cierre anterior: aparecieron **dos reglas más**. La tercera condición de sodio para bebidas sin energía (`≥40 mg/100 ml`), y —la más importante— que **el octógono de calorías exige que YA haya un sello de azúcares o grasas**, porque *"las calorías no son un nutriente crítico, sino una unidad de medida"*. El motor marcaba por energía sola: **es el único error que iba de más** | ✅ Manual pág. 10, 13, 16-17 · `fitogenix-server` `a336d21` |
| 2026-08-31 | **🔴 N-7 nuevo, y es estructural: los octógonos de Fitogenix son necesariamente una aproximación.** El método oficial calcula el nutriente *añadido* desde la **formulación** del producto —receta con porcentajes y fichas técnicas del proveedor (Manual pág. 14-15)—. Fitogenix tiene la **etiqueta**: lista de ingredientes y panel por 100 g. Se puede saber que un ingrediente aporta azúcar, no *cuánto*. **Ninguna corrección de umbrales lo cambia**, y choca con lo que §2.5 le promete al usuario. Es decisión de producto, no nutricional | `nutricion/NUTRICION.md §N5` |
| 2026-08-31 | **B-8 se cierra por corrección, no por trabajo: el bloqueante estaba mal descripto.** Decía que Redis puede servir puntajes viejos porque el prefijo de clave no está versionado. Es cierto lo del prefijo y falso lo del riesgo: `redisService` mete cada entrada en un **sobre** con la `ENGINE_VERSION` y descarta las que no coinciden ✅. El archivo explica por qué el sobre es mejor que la clave versionada. Encontrado al verificar si el bump de v2.2 alcanzaba para invalidar la caché | ✅ `src/services/redisService.ts` |
| 2026-08-31 | **La suite se corrió, por primera vez desde que se documentó: 410 tests en 27 archivos, todos en verde, y `tsc --noEmit` limpio.** Cierra el *"416 tests en verde"* que arrastraba ⚠️ desde el 18/8 sin reproducir. El conteo estático del 28/8 (~345 `it()`) subestimaba: no contaba los casos generados dentro de tablas | `vitest run` + `npm ci` limpio sobre el `package-lock.json` de `d73f378` |
| 2026-08-31 | **Decisión de producto — el octógono resta puntos y no se muestra** (§2.5 reescrita). Resuelve **N-7**, que era estructural: el cálculo oficial parte de la formulación y Fitogenix solo tiene la etiqueta, así que el octógono propio **es y va a seguir siendo una aproximación**. Exhibida como dato contrastable era deshonesta; alimentando un criterio declarado es legítima. Resuelve además **N-1** — ya no hay dos capas que servir con un solo cálculo, queda una. **El contrato no cambia:** `warnings` se sostiene, simplemente nadie lo renderiza. Hallazgo al verificar: el cliente **ya** no lo consumía, y la fuga real estaba en `steps[].detail`, que nombraba los octógonos uno por uno y viaja cacheado en Redis — por eso hay bump aunque el puntaje no cambie: `ENGINE_VERSION` → `ftg-rubric-v2.3` | ✅ grep en `fitogenix-native/src/` (0 referencias) · `scoring/steps.ts` → `applyNutrition` · 421 tests |
| 2026-09-01 | **Registro de hallazgos en `nutricion/NUTRICION.md §N0`, y cinco inconsistencias de numeración corregidas.** `N-6` estaba usado **dos veces** —uno cerrado, uno abierto—: el abierto pasa a `N-10`. `N-2` **no existía** y `09-agente-nutricion.md` mandaba a cerrarlo: se había borrado en vez de cerrarse. Puntero `§N6/N-4` corregido a `§N5/N-4`, la marca 🔧 incorporada a la leyenda y la fila de sodio alineada. La causa raíz no era ninguna de las cinco: **los `N-x` no tenían registro**, así que colisionaban y quedaban colgados en silencio — y el lector de ese archivo es un agente que carga secciones sueltas por puntero. §N0 agrega el índice y tres reglas: no reciclar números, no borrar cerrados, registrar antes de escribir la sección. La lista de tickets de `09-agente-nutricion.md` se rehace: B-11 estaba cerrado y seguía primero | ✅ chequeo automático: 0 referencias colgadas, 0 definiciones duplicadas en los 10 documentos |
| 2026-09-03 | **El esquema vivo se midió por primera vez, y §8 B-6 se describía mal en las dos direcciones.** Daba por pendiente la **014**, que estaba aplicada, y no mencionaba la **008** ni la **012**, que faltaban — más la **013**, que era la cara: `products.score` estaba en `NOT NULL` sin default, y como `runMerge.ts` arma sus payloads con el **mismo** `buildCachePayload` que el request path y upsertea **por lotes**, un solo producto sin puntaje tumbaba el lote entero del ETL dejando nada más que un `console.error`. Las cuatro aplicadas ✅. Hallazgo del método: corriéndolas a mano una por una, **la 012 no entró en el primer intento** y el SQL Editor no avisó nada. Se agrega `npm run verify:schema` (exit 1) y `scripts/sql/diagnostico-esquema.sql` (37 objetos). B-6 pasa de 🔴 a ⚠️: consultar el esquema ya es automático, **aplicar** sigue sin serlo | ✅ `information_schema` de la Supabase de producción · `scripts/verify-schema.ts` · `fitogenix-server` `b5feb19` |
| 2026-09-03 | **Tres rastros del renumerado `a0560ca`, corregidos.** `012_manufacturer_info.sql` se llamaba `010_` en su primera línea y `013_score_nullable.sql` se llamaba `011_`. El tercero estaba **en código**: `scripts/etl/jobs/fixDataQuality.ts` mandaba a aplicar `migrations/010_manufacturer_info.sql`, un archivo que no existe — el job citaba por un nombre inexistente justo la migración que necesita. Las cuatro migraciones tocadas llevan ahora una línea de **ESTADO fechada que apunta al diagnóstico como autoridad en vez de serlo ella**: el encabezado de la 014 ya mintió una vez | ✅ chequeo automático: encabezado == nombre en los 13 archivos · `fitogenix-server` `b816f5a` |
| 2026-09-03 | **ADR-006 escrito** (octógonos, decisión del 31/8). La decisión estaba aplicada en §2.5 y en `NUTRICION.md §N7` pero nunca se registró como ADR; la bitácora iba hasta ADR-005. No cambia estado | `BITACORA_DECISIONES.md` |
| 2026-09-03 | **Nuevo `tareas/FTG-002`.** El guard de frontera de la Fase 3 encontró que `GuideScreen.tsx` transcribe los cuatro cortes de banda (§3.1). Verificando el call site apareció algo peor: la guía marca `badge: 'FITOGÉNICO'` para la banda **50–74**, y `getSello` devuelve `null` en todo el rango 25–74. Es el mismo bug que el comentario de `TIERS` dice haber arreglado —*"un producto de 72 salía 'Bueno' con sello 'Fitogénico'"*— sobreviviendo en el cliente. Falta además la banda `NO_DATA_TIER` (§3.3). Se propone entrarlo a §8 | ✅ `GuideScreen.tsx` · `app/(tabs)/guia.tsx` · `scoring/presentation.ts` → `getSello` |
| 2026-09-03 | **El catálogo estuvo congelado 18 días y nadie lo sabía.** Cadena verificada: `cacheService.ts` es el único escritor de `engine_version` ✅, siempre escribe la constante ✅, esa constante es ≥ v2.1 desde el **2026-08-16** ✅ (`git log -S`), y de 58.076 filas **cero** tenían v2.1 o posterior ✅ → **ninguna fila de `products` se escribió con éxito en 18 días**. Causa: `score NOT NULL` + `runMerge.ts` upserteando por lotes con `buildCachePayload`, que escribe null. Con ~77 % de nulls por lote, ningún lote sobrevivía. **No hubo pérdida irrecuperable** — `runMerge` hace `continue` sin marcar staging cuando el upsert falla ✅, así que las 69.341 filas quedaron en `pending`. Aplicada la 013, el primer lote escribió 200 filas con 155 nulls. Nuevos **B-19** (el job de recompute no existe) y **B-20** (el motor no puntúa alimentos frescos y §1.4 no lo declara). §6 y `FTG-001` quedan **pendientes de recalcular** con la cola drenada: no se reescriben con estimación | ✅ `git log -S` en `constants.ts` · `npm run verify:schema` antes/después · `npm run etl:stats` · `runMerge.ts` |

| 2026-09-03 | **La carga del catálogo terminó y `§6.3` se reescribió con números medidos.** 81.450 productos · 20.091 sin puntaje · `pending` en 0 · 235.746 filas de staging, de las cuales **64,3 % entraron sin ingredientes**. Se documenta que el 24,7 % de "sin puntaje" **engaña**: solo las 24.779 filas escritas el 3/9 pudieron guardar un null, así que la tasa real sobre lo nuevo es **81 %** — y la del catálogo entero no se conoce hasta que corra el recompute (**B-19**). Hallazgo del método: `npm run etl:stats` reportó `TOTAL: 156.000` sobre un total real de 235.746 porque `paginateRows` devuelve lo que alcanzó a juntar cuando la query expira, y `stats.ts` lo imprime como total. Cuarto bug del día de la misma familia: resultado parcial presentado como completo | ✅ `npm run verify:schema` · SQL directo sobre `products_staging` · `staging.ts` → `paginateRows` |

| 2026-09-08 | **`§8` pasa a tener subsecciones, y era el 23 % de todo el contexto.** Medido: `§8` pesaba 9.834 B, lo citaban 9 de 10 agentes y aportaba **88.506 B de los 375.987** que consumía el set. Cada bloqueante abierto es ahora `§8.<n>`; los 8 cerrados viven juntos en `§8.0`, verbatim. Un puntero `§8` a secas trae **467 B** en vez de 9.834. Se convirtieron **46 citas** en 14 documentos. Contexto por invocación de los 10 agentes: **375.987 → 320.069 B (−61 % contra los 800.323 sin cargador)**. Hallazgo al convertir: **7 de las 21 filas tenían 3 columnas en una tabla de 4** — B-6 a B-12 venían mostrando el dueño bajo la cabecera *Estado*. Con campos nombrados (`**Quién decide:**`) el error desaparece. Las 21 filas se preservaron íntegras, verificado por contención de texto | ✅ 0 punteros colgados · 0 umbrales transcritos · 68 tests en verde |

| 2026-09-08 | **`§5.4` se contradecía con `§8.0` B-8, y `§5.4` era la equivocada.** Afirmaba que *"Redis sí puede servir un puntaje viejo"* y que la invalidación *"no está aplicada"*. Verificado en el call site: `setInRedis` guarda cada entrada en un **sobre** con la `ENGINE_VERSION`, y `getFromRedis` → `unwrapCachedProduct` devuelve `null` si no coincide — es MISS, y se loguea como `redis_stale_engine_version`. Era cierto sobre el prefijo estático y **falso sobre el riesgo**. B-8, cerrado el 31/8, ya lo decía bien. La deriva vivió una semana con las dos versiones en el mismo documento. Lo encontraron **dos agentes por separado**, cada uno verificando contra el código, en el mismo día | ✅ `src/services/redisService.ts` → `setInRedis` · `unwrapCachedProduct` · `getFromRedis` |

| 2026-09-11 | **`§1.6` y `02-agente-frontend.md` puestos al día contra el rediseño del cliente.** Entre la verificación anterior (`b7715b8`) y hoy (`c63c3cc`) entró un PR de **3.114 líneas**: pantalla **Feedback** nueva, `ScoreBreakdownSheet.tsx` borrado, tab bar con indicador deslizante y swipe entre pestañas, microinteracciones, e Inicio y Resultado reescritos casi enteros. Se corrigieron dos afirmaciones falsas en `02`: decía que `scan_failed` *"todavía no existe"* (existe desde el 31/8; lo que falta es el sink, `§8.17`) y daba `ScoreBreakdownSheet.tsx` por pendiente de borrar. **El dato que más pesa:** el rediseño tocó `GuideScreen.tsx` (+11/−3) y **no vio el defecto de `FTG-002`** — el `TIERS` local y el sello prometido en la banda 50–74 siguen ahí. La documentación se despegó del código en **cinco días**, y un humano pasó por el archivo sin verlo | ✅ `git diff dce8400..c63c3cc` · `ls src/screens` · grep de `ScoreBreakdownSheet` y `ftgEngine` en `src/` |

| 2026-09-11 | **El harness existe: permisos, CI y los 10 agentes registrados como subagentes.** Hasta hoy había 334 KB de contexto disciplinado y ninguno llegaba a la herramienta con la que se escribe código. `.claude/settings.json` en los tres repos convierte en permiso lo que era prosa: `deny` sobre secretos y `push --force`, y **`ask` antes de escribir `CONTEXT.md`** — *"único escritor: el Orquestador"* deja de depender de que el agente obedezca. En este repo además `deny` sobre `PODA_REPORTE.md` y `REALINEACION_REPORTE.md`, que esta misma sesión tocó por accidente. **CI nueva en `fitogenix-server`**, que no tenía ninguna: `tsc --noEmit` + tests, con Node en `.nvmrc` y `engines.node >=22.0.0` **medido** (lo impone `@supabase/supabase-js`; `vitest` acepta `^20 \|\| ^22 \|\| >=24`) — cierra la mitad de `§8.9`. CI nueva acá: los 68 tests más un paso que falla si un puntero `§X` no resuelve. **`.claude/agents/`**: 10 registros de ~2,3 KB que no repiten nada — apuntan a su `.md` y declaran `tools` y `model` según `PROPUESTA_grafo_fase2.md` sección 5. Los que no implementan (`architect`, `nutrition`, `qa`) no tienen `Edit` ni `Write`; el orquestador solo lee. **Límite declarado:** los guards de frontera viven en este repo y correrlos desde los otros exige checkout multi-repo; vendorizarlos sería la segunda copia que ellos existen para impedir | ✅ YAML de los 2 workflows y los 10 frontmatter parseados · los 10 punteros a prompts resuelven · 0 umbrales transcritos · 68 tests |

| 2026-09-18 | **A7 — el vínculo código→contexto, verificable.** `orquestacion/fitogenix/punteros.py` + 11 tests: un puntero al SSOT citado desde el código que no resuelve ahora falla. Cubre `CONTEXT.md §X`, `NUTRICION.md §Nx`, `ADR-00N`, rutas de archivo citadas, y **la cita por número de línea**. **Hallazgo que obligó a diseñarlo distinto:** `§X` significa **dos cosas** en este proyecto. En `fitogenix-server` hay 46 apariciones de `steps.ts §2`, 13 de `steps.ts §4.7` y 9 de `steps.ts §4.5` que **no son de `CONTEXT.md`** sino de la rúbrica del motor (`scoring/steps.ts` → *"FITOGENIX — §2: los pasos del cálculo"*); `CONTEXT.md §4` ni siquiera tiene una quinta subsección. Por eso el verificador **solo valida punteros calificados** — validar un `§` pelado serían 46 falsos positivos el primer día. Que haya dos numeraciones con la misma notación es deuda de **backend** + **orchestrator**. Se suma `orquestacion/verificar.py`: **un comando** que corre punteros del set, punteros del código, guards y cobertura, con exit 1. Los guards y el verificador encontraban cosas reales pero dependían de que alguien se acordara de correr tres comandos distintos. **Barrido inicial:** 0 punteros rotos en los dos repos. 3 hallazgos de guards — los dos conocidos de `GuideScreen` (FTG-002) y uno nuevo: 🔴 `migrations/013_score_nullable.sql` transcribe los cortes del sello en un `COMMENT ON COLUMN` **que ya está aplicado en la base**, así que los umbrales viven en la metadata de Postgres. Viene de `c88c71d`. Corregirlo pide una migración nueva: es del **architect**. Cobertura de punteros en `domain/` y `routes/`: **3/31 en server, 1/2 en native** — se informa, no falla | ✅ 79 tests · `python verificar.py` sobre 113 + 74 archivos reales |


### Pendiente inmediato (fuera de este documento)

**Poda de los 8 agentes a punteros §X.** Este documento tenía que existir primero. Cada
agente pierde su `## El producto: Fitogenix`, su copia del stack y su copia de las bandas,
y en su lugar cita `CONTEXT.md §1`, `§5`, `§3`. Es la cura de C-06 — la duplicación en
ocho lugares que causó C-01 y C-03.

| 2026-09-18 | **P0 — el HitL deja de depender de que el modelo confiese, y el primer contrato real encuentra un agujero en el barrido.** (1) `orquestacion/fitogenix/det.py` + 17 tests: los **7 chequeos deterministas** de incertidumbre que `PROPUESTA_grafo_fase2.md` sección 2 pedía y que **no existían** — hasta hoy `hay_que_preguntar()` solo miraba lo que el modelo declaraba, que es justo el agujero que el diseño dice tapar (*"declarar menos incertidumbre es el camino más corto a no ser interrumpido"*). Incluye `presupuesto-excedido`: un Brief no puede citar `§X` entero cuando existen `§X.Y`, ni arrastrar más de 12 KB. (2) **Golden set #1** — `tests/golden/ftg_002.py`: la salida real del arquitecto contra FTG-002, producida **sin permiso de leer `orquestacion/`** para que el test fuera honesto. El objeto valida, `hay_que_preguntar` da True (6 supuestos, 5 decisiones abiertas ⇒ el pipeline se habría interrumpido) y `escala_a_opus` da True. **Lo que el contrato real rompió:** `_DCE` exigía `dado` literal y rechazaba "Dada una banda cualquiera / Cuando / Entonces" — castellano correcto — así que caían 3 de 9 campos por gramática y ninguno por criterio; corregido a `dad[oa]s?`. **Lo que no entra y queda fijado como deuda en el propio golden:** las 3 puntas nuevas del contrato (`routes/scoring/bands.ts` y sus dos espejos) no se pueden declarar porque `PUNTOS_DEL_CONTRATO` es constante; `ReglaDeValidacion.puntero` es un `str` y el arquitecto dio dos por regla, lo que hace que `det.verificado_sin_ruta` levante 5 hallazgos de los que **4 son artefacto del schema**; `decisiones_abiertas` pierde el dueño que el arquitecto le puso a cada una. (3) **El hallazgo que paga la sesión:** el arquitecto dejó como supuesto ⚠️ que el barrido podía no ver `HomeScreen.tsx`. Verificado: **era cierto**. `_ETIQUETA_DE_BANDA` no llevaba `IGNORECASE`, el motor escribe `EXCELENTE` y el cliente `"Excelente"`, y `scoreLabel`/`scoreColor` —una tabla de cortes completa, alcanzable desde `HistoryCard`— daban **0 hallazgos**. El criterio A-2 de FTG-002 (*"el barrido completo da 0 hallazgos"*) habría dado **verde con el defecto vivo**. Arreglarlo a secas metía 4 falsos positivos (prosa española con "Moderado", `NOVA 4`, `omega-6`, una cita `rubric/index.ts §5`), así que el corte suelto pasa a exigir **dos comparaciones** cerca de una etiqueta, o **un argumento de llamada**. (4) `verificar.py` fijaba los repos en `~/<nombre>` y en esta máquina cuelgan de `~/mnt/`: **salteaba en silencio 3 de sus 4 verificaciones y salía 0 igual**; ahora los busca vía `config.SETTINGS`. (5) El mismo verificador reportaba como rotos los `steps.ts §4.5`/`steps.ts §4.7` que el `CHANGELOG` cita **para explicar que no son de `CONTEXT.md`**: ahora distingue las dos numeraciones. | ✅ 123 tests · `verificar.py` sobre 187 archivos reales: 4 hallazgos, 0 falsos positivos |
