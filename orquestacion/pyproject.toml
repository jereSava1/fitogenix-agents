[project]
name = "fitogenix-orquestacion"
version = "0.1.0"
description = "Pipeline agéntico de Fitogenix — contratos, guards y grafo"
requires-python = ">=3.10"
dependencies = ["pydantic>=2.7"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
